import { Container, H1 } from "../components/ui";
import { trpc } from "../utils/trpc";

const SearchPage = () => {
  return (
    <Container>
      <H1>Search</H1>
      <ul>{}</ul>
    </Container>
  );
};

export default SearchPage;
